<p style="color: green"><%= notice %></p>

<% content_for :title, "Steps" %>

<div class='mb-5'>
  <div class='d-flex justify-content-between align-items-center'>
    <h1>Steps</h1>
    <%= link_to "New step", new_step_path, class: 'btn btn-primary' %>
  </div>
  <div class='clearfix'></div>
</div>

<div id="steps" class='row row-cols-1 row-cols-md-2 row-cols-xxl-3 mb-3 gx-3 gy-5 justify-content-center'>
  <% @recipes.each do |recipe| %>
    <%= render 'steps/recipe', recipe: recipe %>
  <% end %>
</div>

<%= link_to "New step", new_step_path %>
